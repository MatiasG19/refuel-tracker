version: 2
updates:
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'monthly'
    groups:
      patch-dependencies:
        update-types: 
          - "patch"
    labels:
      - 'dependencies'

  - package-ecosystem: 'npm'
    directory: '/src-capacitor'
    schedule:
      interval: 'monthly'
    groups:
      patch-dependencies:
        update-types: 
          - "patch"
    labels:
      - 'dependencies'

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    groups:
      patch-dependencies:
        update-types: 
          - "patch"
    labels:
      - 'dependencies'
